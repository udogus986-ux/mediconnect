[build]
builder = "nixpacks"
buildCommand = "npm install && node node_modules/typescript/bin/tsc"

[deploy]
startCommand = "node dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3